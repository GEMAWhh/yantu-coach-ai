# AGENTS.md｜所有研发智能体的强制工作规则

## 1. 开始任务前

1. 读取当前 Issue、相关产品规范、验收矩阵和 ADR。
2. 明确目标、非目标、允许修改的目录和退出条件。
3. 检查依赖任务是否已经合并。
4. 不确定时先提出决策请求，不得依据常识补造业务规则。

## 2. 修改范围

- 一个 PR 只解决一个核心问题。
- 禁止同时进行大规模重构与新增业务功能。
- 禁止修改与 Issue 无关的文件。
- 修改数据模型、API、业务规则时必须同步更新契约文档和测试。
- 不得把演示数据写入正式种子数据。

## 3. 测试纪律

- 新增业务逻辑必须有单元测试。
- 修复缺陷必须先增加能够复现缺陷的测试。
- 禁止删除、跳过或弱化测试以使 CI 通过。
- 禁止仅测试“正常路径”；至少覆盖一个失败或边界路径。
- 数据迁移必须测试：新库初始化、旧库升级、失败回滚和数据一致性。

## 4. 数据与 AI 安全

- AI 输出只能进入草稿；未经确认不得写入正式业务表。
- API Key 不得出现在前端、日志、测试快照或提交历史中。
- 正式数据不得用于开发测试。
- 文件路径必须由服务端生成，禁止使用用户提供路径直接读写。
- 不得让大模型直接执行 SQL、文件删除、数据库迁移或发布操作。

## 5. 学习系统不可破坏的业务规则

1. 上传完成 ≠ 整理完成。
2. 整理完成 ≠ 学习完成。
3. 学习完成 ≠ 稳定掌握。
4. 阅读、听课和拍照最多支持进入“已接触”。
5. 原题即时重做正确不能直接标记错题已解决。
6. 稳定掌握必须允许因抽测失败或重复错因而回退。
7. 选择“只学某科”时不得插入其他科目。
8. 前置知识不足时必须阻止或降级后续任务。
9. 计划不得排满全部可用时间，必须保留机动时间。
10. 任何状态变化必须可解释、可追溯并记录规则版本。

## 6. PR 提交前

必须完成：

- 格式化、静态检查、类型检查；
- 单元测试、集成测试和相关 E2E；
- 检查数据库迁移；
- 更新文档；
- 填写 PR 模板；
- 附上 UI 截图或 API 示例；
- 写明风险、回滚方式和未完成项。

## 7. 禁止行为

- 直接向 `main` 推送；
- 自己审批并合并自己的 PR；
- 绕过 CI；
- 未备份修改正式数据库；
- 将模拟 OCR 或模拟 AI 结果标记为真实结果；
- 用“看起来可用”代替验收标准；
- 因时间不足擅自缩减安全、备份或确认机制。
