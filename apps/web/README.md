# Web

Vue 3 + TypeScript 前端骨架。当前阶段只提供五个一级入口的静态应用外壳，用于验证构建、类型检查和单元测试。

## 本地运行

```powershell
cd apps/web
npm ci
npm run dev
```

## 检查

```powershell
npm run typecheck
npm run lint
npm run test:unit
npm run build
```
